NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
NEXT_PUBLIC_WS_URL=ws://localhost:3000
# Sentry DSNs are write-only ingestion endpoints, not secrets — safe as a
# client-visible NEXT_PUBLIC_ var. Leave blank to no-op error reporting.
NEXT_PUBLIC_SENTRY_DSN=
